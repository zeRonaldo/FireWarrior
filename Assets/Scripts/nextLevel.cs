using UnityEngine.SceneManagement;
using UnityEngine;

public class nextLevel : MonoBehaviour {
	public string nextLevelName;
	public float deltaTime;

	/*void Start() {
		yield return WaitForSeconds (deltaTime);
		SceneManager.LoadScene (nextLevel);
	}*/

}
